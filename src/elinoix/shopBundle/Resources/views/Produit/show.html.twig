{% extends '::base.html.twig' %}

{% block title %}
Affichage d'un Produit
{% endblock %}

{% block page_title %}
{{ entity.nom }}
{% endblock %}

{% block page_subtitle %}{% endblock %}

{% block body -%}
    <div class="row">
        <div class="col-sm-6"><img src="{{ asset('upload/' ~ entity.image.id ~'/' ~ entity.image.image)}}"/></div>
        <div class="col-sm-6"></div>
    </div>

    {#<table class="record_properties table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ entity.poids }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ entity.prix }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ entity.quantite }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td><img src="{{ asset('upload/' ~ entity.image.id ~'/' ~ entity.image.image)}}"/></td>
            </tr>
        </tbody>
    </table>#}

        <ul class="record_actions">
    <li>
        <a href="{{ path('produit') }}">
            Retourner à la liste des produits
        </a>
    </li>
    {#<li>
        <a href="{{ path('produit_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>#}
    {#<li>{{ form(delete_form) }}</li>#}
</ul>
{% endblock %}
